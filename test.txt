(define myfunc (
    lambda (x) 
        (println x)
        (+ x x)
))

(myfunc 10)

(println (+ 45.2 (System.Double\Parse (readln))))

(define sgn (lambda (x) (cond 
    (> x 0) 1         
    (< x 0) -1
    (= x 0) 0
)))

(sgn -64)